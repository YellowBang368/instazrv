<!DOCTYPE html>
<html>
  <head>
    <title>Inst</title>
    <%= csrf_meta_tags %>

    <link href="https://fonts.googleapis.com/css?family=Open-Sans|Nunito+Sans|Open+Sans|Roboto|Work-Sans|Mukta|Heebo" rel="stylesheet">
    <%= stylesheet_link_tag    'application', media: 'all' %>
    <%= javascript_include_tag 'application' %>
    <meta name="viewport" content="width=device-width, initial-scale=1, maximum-scale=1, user-scalable=no" />

  </head>

  <body>
    <%= render "layouts/header" if controller_name != "sessions" %>
    <%= link_to "exit", destroy_user_session_path, method: "DELETE" if user_signed_in? %>
    <a class="google" href="https://google.com">google</a>
    <%= yield %>
    <%= yield :crop %>
  </body>
</html>
