<% if user_signed_in?  %>
  <% unless current_user == user %>
    <% if follows(user) %>
      <%= link_to "Unfollow", relationship_user_path(user), method: "DELETE", remote: true, class: "unfollow-button" %>
    <% else %>
      <%= link_to "Follow", relationship_user_path(user), method: "POST", remote: true, class: "follow-button" %>
    <% end %>
  <% end %>
<% else %>
  <%= link_to "Follow", relationship_user_path(user), method: "POST", remote: true, class: "follow-button" %>
<% end %>
